// This component has been removed as part of simplifying the password reset flow.
// Password reset is now handled entirely through Supabase email callbacks.

import React from 'react';

// Dummy component for backward compatibility
export function PasswordResetConfirm() {
  return null;
}